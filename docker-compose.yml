version: "3.8"

services:
  healthcare-ai:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./vector_store:/app/vector_store
    env_file:
      - .env
    restart: unless-stopped
